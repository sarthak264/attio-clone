interface Icon {
  className?: string
}

interface tagContent {
  headline: string
  link: string
}

interface flagContent {
  headline: string
  link: string
}

export type { Icon, tagContent, flagContent }
